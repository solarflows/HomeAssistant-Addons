# alist-tvbox 版本检查配置
addon: alist-tvbox
source: github
repo: power721/alist-tvbox
tag_prefix: ""           # 上游 tag 无 v 前缀，如 1.24.0
build_args:
  ALIST_TVBOX_VERSION: "${version}"
changelog:
  source: release_body  # 上游 release note 既是 changelog
  mode: prepend         # 添加到开头
